{
  "name": "@workspace/api-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080} --reload",
    "build": "echo 'Python backend: no build step required'",
    "typecheck": "echo 'Python backend: no typecheck step'"
  }
}
